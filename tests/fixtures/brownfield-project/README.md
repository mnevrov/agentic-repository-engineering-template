# Existing Labels Service

This fixture represents a repository that existed before adoption of the template. Production code lives in `legacy_app/`, tests in `qa/`, and verification is exposed through `scripts/`.
